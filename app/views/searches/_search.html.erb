<%= form_with url: search_path, method: :get do |f| %>
  <%= f.text_field :word %>
  <%= f.select :content, options_for_select([["User"],["Book"]]) %>
  <%= f.select :type, options_for_select([["完全一致"],["前方一致"],["後方一致"],["部分一致"]]) %>
  <%= f.submit "検索" %>
<% end %>